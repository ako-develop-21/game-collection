# Challenge 03: Particle Vacuum

`src/components/animation/ParticleFlow.vue` にアニメーションを実装してください。

## 目標
- 周囲のパーティクルが中心に向かって吸い込まれ、消えてはまた現れるような動きを作ってください。
- 各パーティクルに `animation-delay` を設定し、動きにバラツキを出してください。

## 条件
- `opacity` と `transform: scale() / translate()` を組み合わせて使用すること。
