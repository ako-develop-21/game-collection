# プロジェクト方針

## 新規機能実装時

- HOME画面から遷移できるようにする。また、各画面からHOMEへの導線も用意すること。
- 動作保障環境はWindows/Google Chromeのみとする。
- 画面サイズは基本的に小さく表示して使用するため、小さい画面でも問題なく表示・使用できるUIを心がけること。
- 大規模な実装になる場合は、work/ディレクトリ配下に(機能名)_dashboard.mdを作成し、そこに実装方針および今後のタスクを記載すること。
- 実装機能が多岐にわたる場合は、実装をフェーズごとに区切り、フェーズごとに実装・修正・確認を行っていくこと。
- 実装・修正後は(機能名)_dashboard.mdを更新し、最新の状況を記載すること。
- 動作確認の際、localhostのport番号5173は使用しないでください。他プロジェクトが使用中です。
- 実装済みロジックの検証は助かりますが、実際にブラウザを稼働させた上での動作確認は、コストが重いので不要です。

## 修正が膨大になる場合の対応

修正が数百行に及ぶ場合、AIの修正処理が`generationg...`で止まる傾向があります。
実際に修正にとりかかる中で、修正量が膨大になることが判明した場合は、
たとえユーザから「修正を実行して」というような指示を受けていても、修正処理を一度停止してください。

代わりにフェーズごとに分割した修正案を提示し、ユーザの承認を得てから、
フェーズごとに修正処理を実行してください。

中断する目安は、修正が300行以上に及びそうな場合、としてください。

## 機能ごとの実装方針

### ドミニオン（dominion）

- **ナレッジの活用** : 機能の実装・修正前に`work/dominion_knowledge.md`を参照し、過去の知見を活用すること
- **ナレッジ蓄積の推進** : 不具合が発覚して修正した直後は、ユーザに`work/dominion_knowledge.md`の更新を促すこと
- **手動デバッグの推奨** : 新規カードを実装した場合は、useDominion.tsのDEBUG_DECKに追加したカードをすべて投入し、手動で動作確認を行うこと
- **カード追加時の注意** : カード追加時は、`src/components/dominion/CardSelectionModal.vue`のカードリストにも追加すること
